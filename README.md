# Passworld
pour compiler:
```bash
$ gcc -o main -Wall -Werror main.c
```
ou 
```bash
$ make
```

pour executer:
```bash
$ ./main ./tmp
```
./tmp est le fichier de mot de passe (pour l'instant non chifer)

